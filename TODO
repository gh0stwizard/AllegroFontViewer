* read/write settings from/to the ini-file
* show a size of the font near a text when drawing it
* an abillity to type any text when displaying the choosed font
* customization support:
	* colors (directory listing, font drawing)
	* window resize + fullscreen support
	* keyboard shortcuts
	* a gamepad support
	* font & its size for UI (dir. listing, fileinfo)
V fast navigation via PgUp/PgDown keys
	* abillity to customize this setting
* support for favorite fonts
* better error notification support
V when navigating between dirs remember previous selected directory position
* color highlight support for known allegro's font extensions in browsing mode
