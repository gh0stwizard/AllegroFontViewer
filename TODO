			Legend: V is done, * is in queue, ? is under a question

V read settings from/to the ini-file
	V start directory support
* create an interactive menu to change settings
	* save settings to ini-file
V show a size of the font near a text when drawing it
V an abillity to type any text when displaying the choosed font
* customization support:
	V colors (directory listing, font drawing)
	* window resize + fullscreen support
	* keyboard shortcuts
	* a gamepad support
	V font & its size for UI (dir. listing, fileinfo)
V fast navigation via PgUp/PgDown keys
	V abillity to customize this setting
* add favorite fonts menu
	? add abillity to create notes/comments for the choosed fonts
V better error notification support
	* use al_draw_multiline_text()
V when navigating between dirs remember previous selected directory position
V color highlight support for known allegro's font extensions in browsing mode
V create an icon for the app.
* ask user confirmation when trying to load image files (bmp, png, etc)
* add a magnifier for the view font mode
V fix bug when browser scroll speed = 0
V fix bug when reload configuration file (get/set absolute path to the files)
V add help menu
	* improve help menu (show vim keys, list all keys per one action, etc)
* add abillity to run the program without any configuration files
* fix font's drawing using a line height of a font (see dafont/phatone)
